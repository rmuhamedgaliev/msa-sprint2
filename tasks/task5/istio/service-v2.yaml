apiVersion: v1
kind: Service
metadata:
  name: booking-service-v2
  labels:
    app: booking-service
    version: v2
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: booking-service
    version: v2
